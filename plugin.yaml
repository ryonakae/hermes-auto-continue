name: hermes-auto-continue
version: 1.0.0
description: Gateway-only auto-continue after Hermes max-iteration summaries.
kind: standalone
provides_hooks:
  - pre_gateway_dispatch
  - post_llm_call
provides_commands:
  - auto-continue
